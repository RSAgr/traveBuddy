services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: travebuddy
      POSTGRES_USER: travebuddy
      POSTGRES_PASSWORD: travebuddy_dev
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
volumes:
  postgres_data:
